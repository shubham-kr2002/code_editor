import React from 'react';

/**
 * Navbar Component
 * 
 * The top navigation bar of the application using Material UI.
 * Currently not used in the simplified UI.
 */
const Navbar = () => {
  return null;
};

export default Navbar; 